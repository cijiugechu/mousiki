[package]
name = "mousiki-ogg"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Pure Rust Ogg bitstream/container primitives for mousiki."

[features]
default = []
std = []

[dependencies]

[dev-dependencies]
